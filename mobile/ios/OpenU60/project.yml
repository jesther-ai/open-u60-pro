name: OpenU60

options:
  bundleIdPrefix: com.openu60
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "26.6"
  generateEmptyDirectories: true

settings:
  base:
    # Language mode, independent of the Swift compiler bundled with Xcode.
    SWIFT_VERSION: "5.0"

targets:
  OpenU60:
    type: application
    platform: iOS
    sources:
      - path: OpenU60App.swift
      - path: Core
      - path: Features
      - path: Navigation
      - path: Resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.openu60.app
        INFOPLIST_FILE: Resources/Info.plist
        GENERATE_INFOPLIST_FILE: false
        CODE_SIGN_STYLE: Automatic
        TARGETED_DEVICE_FAMILY: "1,2"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon

schemes:
  OpenU60:
    build:
      targets:
        OpenU60: all
    run:
      config: Debug
    archive:
      config: Release
